<%= simple_form_for @item_price, html: { class: 'form-horizontal' } do |f| %>
  
  <!-- Modal Trigger -->
  <a class="waves-effect waves-light btn modal-trigger" href="#modal1">Edit Price?</a>
  <!-- Modal Structure -->
  <div id="modal1" class="modal">
    <div class="modal-content">
      <h4>Enter A New Price For This Item</h4>
      <div class="input-field col s3">
        <center><%= f.label :price %></center>
        <%= f.text_field :price %>
      </div>
      <%= f.input :start_date, as: :hidden, input_html: {value: Date.today } %>
      <%= f.input :end_date, as: :hidden, input_html: {value: nil } %>
      <%= f.input :item_id, as: :hidden, input_html: {value: @item.id } %>
    </div>
    <div class="col s12">
      <div class="form-actions">
        <%= f.submit nil, :class => 'button radius' %>
        <%= link_to 'Cancel', item_path(@item), :class => 'button alert radius' %>
      </div>
   </div>
  </div>
<%end%>

